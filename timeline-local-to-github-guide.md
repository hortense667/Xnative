# ローカル作成→GitHub公開 はじめてガイド

このガイドは、**まずローカルJSONで安全に試作し、確認後にGitHubへ公開**する手順です。  
「最初からGitHubに保存するのは不安」という人向けです。

## 1) 事前準備

- 年表ビューアを `?editmode=ON` 付きで開く
- 可能なら Chrome / Edge の最新版を使う（ローカルJSON上書き保存が安定）
- 作業用フォルダを1つ作る（例: `my-timeline-work/`）

## 2) 初期データを作る（ゼロから始める）

方法は2つあります。初心者は A 推奨です。

- A. `timeline_initial_00.json` をコピーして、`timeline_myproject_01.json` などにリネーム
- B. 空に近いJSONを自作（構造ミスしやすいので非推奨）

ポイント:

- ファイル名は `timeline_` 始まりにする
- 文字コードは UTF-8

## 3) ローカルモードで読み込む

1. 画面の「設定」を開く  
2. データソースを「ローカルJSON（PC上）」に設定  
3. 「表示中の全項目をクリアしてローカルから読み込み」で、作成した `timeline_myproject_01.json` を選ぶ

設定 → データソースに **読み込み／保存先の年表JSON** が表示されます。別バージョン（`_02` / `_03` など）と取り違えないよう、編集前にここを確認してください。  
URL は `?datasrc=local` まで指定可能ですが、**ファイル名の指定や自動読み込みはできません**。

## 4) 動かしながら最初の編集をする

まずは少量で確認します。

- 左カラム空白クリックで新規追加
- 1〜3件作成して、編集画面の「保存」
- 詳細一覧で並び替えて「閉じる」
- 1件削除してみる

この時点で、ローカルモードでは次が起きます。

- ヘッダーのボタン表示が「保存」
- 編集保存 / 並び替え確定 / 削除でもローカルJSON保存
- 初回のみ保存先確認、以降は同じファイルへ上書き（対応ブラウザ）

## 4.5) 本格作成前に初期化して自分データへ切替

`timeline_initial_00.json` は操作把握には便利ですが、そのままだと既存項目が多く作業しづらい場合があります。  
自分の年表を本格的に作る段階で、次を推奨します。

1. 「初期化（表示中の全項目とジャンル、時代区分をクリア）」を実行  
2. CSV/TSV の「ロード」で自分のデータを投入  

補足:

- 初期化はリモート（GitHub）を書き換えません
- 画面内データ・設定をリセットする強い操作なので、必要なら事前にスナップショット保存

## 5) ローカルで品質確認

- 検索で意図した項目が引けるか
- 年・終了年・ジャンル・重要度が崩れていないか
- URLプレビューが問題ないか
- スナップショット保存/復元が動くか
- 年表名・著作権などの `metadata` をJSONで更新できているか

推奨:

- まとまった編集前に「スナップショット保存」
- 節目ごとにファイル名を増やしてバックアップ（例: `timeline_myproject_02.json`）

## 6) GitHub公開へ切り替える

1. GitHub でリポジトリ作成（公開/非公開は用途に応じて選択）  
2. ローカルで確定した JSON をリポジトリへ配置（例: ルートに `timeline_myproject_01.json`）  
   - 公開前に `metadata.title/copyright/license` を確認  
3. 必要なら `xnative_link_map.json` に `xid` を追加  
4. ビューアURLを GitHubモードで作成して確認

例:

`?datasrc=github&owner=<あなたのID>&repo=<repo名>&filePath=timeline_myproject_01.json`

## 7) 公開後の運用パターン

- 日常の試作: データソースをローカルJSONにして実験
- 公開反映: 安定版だけGitHub JSONへ反映
- 複数人編集: GitHubフロー（Issue / PR）へ寄せる

## よくある迷いどころ

- Q. ローカルモードなのにGitHubへ保存される？  
  - A. されません（ローカルJSON保存のみ）

- Q. どこで保存される？  
  - A. 初回に選んだ保存先ファイルです（対応ブラウザ）。非対応環境はダウンロード保存です。設定 → データソースの「読み込み／保存先の年表JSON」で確認できます。

- Q. URL でローカルファイルを指定できる？  
  - A. `?datasrc=local` のみです。ファイル名は URL では指定できず、自動読み込みもできません（ブラウザの制限）。必ず「ローカルから読み込み」でファイルを選んでください。

- Q. いきなり公開して大丈夫？  
  - A. まずローカルで「追加・編集・並び替え・削除」を一巡してから公開が安全です。

---

関連:

- `users_guide.md`
- `timeline-sharing-github.md`
- `README_XnativeTimeline.md`
